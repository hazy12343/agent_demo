# 模块一：Context / Performance
## 1.大模型面对第一轮长窗口或多模态输入时，first token 会显著变慢。有什么快速/低成本/用户体验也不差的方案？从5-10秒稳定压缩到2秒。
### 回答：
首先，需要明确延迟究竟产生在哪个环节。

大模型的一次自回归推理可以划分为两个阶段：Prefill（预填充）与 Decode（逐 token 生成）。首 token 的延迟，本质上是 Prefill 阶段的耗时。
在 Prefill 阶段，系统会将全部输入 token（在多模态场景下还包含图像切片）一次性送入模型，完成所有层的 KV Cache 计算，并推导出首个输出 token 的
概率分布。其中，计算开销最大的部分是自注意力机制，其运算量与输入序列长度 L 的平方成正比。

当输入长度达到数万乃至十几万 token 时，平方级的计算增长会迅速消耗算力，加之内存带宽的限制，便导致了观察到的 5 到 10 秒延迟。
因此，要将首 token 延迟压缩至 2 秒，核心路径有两条：其一，直接缩短有效序列长度 L，从根本上削减计算量；其二，通过并行或提前计算来隐藏等待时间，使延迟对用户不可见。

下面我将逐一介绍具体的实现方案。

方案一：提示压缩

这是当前性价比最高且最为直接的路径。其原理在于，既然计算成本与 L 的平方强相关，我们可以引入一个极轻量的模型，对原始提示中的冗余、寒暄及低信息密度 token 进行剔除，
将原始输入压缩为仅保留核心信息的浓缩版本。若能实现 5 到 10 倍的压缩，理论上前述注意力延迟可降至 1/25。即使计入前馈网络等线性部分，实际首 token 延迟
压缩到原来的十分之一以内也是常见的。10 秒缩减至 1 到 2 秒完全可期。

具体实现可采用 LLMLingua-2，其方法是用一个微型语言模型对提示中每个 token 进行重要性评分，仅保留高重要度 token，压缩率可达 5 至 20 倍。该压缩模型
可在 CPU 上运行，单次耗时低于 100 毫秒，几乎不引入额外延迟，且在多轮对话、长文档及 RAG 场景中，回答质量的折损极低。此外，也可采用选择性上下文或递归摘要的
方法，由小模型先生成抽取式摘要再交由大模型处理。

在多模态场景下，视觉 token 压缩同样关键。例如，FastV 或 Token Merging 等技术能够对图像中信息贡献较低的 patch 进行剪枝或合并，将单张图像的 token 数
从数百个压缩至 64 到 100 个；对视频则可通过选取关键帧并逐帧压缩，实现输入长度的量级式缩减。

此方案成本极低，仅需一个小型模型，用户体验完全无感知，是当前压缩首 token 延迟最经济有效的途径。

方案二：流式预填充

现在换一个思路：如果用户的输入是逐段到达的，则无需等待完整提交即可启动计算。推理引擎可支持增量预填充。前端在用户打字、语音转写 或粘贴长文本时，将内容分块
通过流式传输至服务端，服务端每收到一个数据块便立即进行 Prefill 并更新 KV Cache。当用户最终提交时，绝大部分输入已完成预计算，仅需对最后很小的一段尾巴
（约数百 token）完成收尾 Prefill，即可输出首 token。延迟因此从 O(L²) 降为 O(Δ²)，通常可控制在几百毫秒。

工程落地要求前端监听输入事件并进行分块传输，服务端则需采用支持连续批量处理与增量预填充的推理框架，如 vLLM、SGLang 或 LMDeploy。该方案无任何额外模型
成本，仅需调整前后端逻辑，并可完美与提示压缩结合，将首 token 延迟进一步压缩至 1 秒左右。

方案三：推测式首 token

当手头拥有一个参数规模极小的 draft 模型时，可采用推测式生成来隐藏大模型的延迟。用户输入到来后，系统并行运行两条路径：draft 模型迅速
完成 Prefill 并产生首 token（耗时仅 50 至 300 毫秒），直接流式返回给用户，营造瞬时响应；同时，大模型在后台进行完整 Prefill。待大模型首 token 输出
后，与 draft 结果比对，若一致则无缝衔接；若不一致，则用大模型 token 替换界面文字，即“闪烁修正”，通过将预测态文字置为浅灰色等 UI 手段，该替换对用户几乎无感。

此方案额外显存开销小，推理成本极低，且可复用推测解码已有的 draft 模型。用户感知的首 token 延迟即 draft 模型的延迟，大模型的完整 Prefill 时间被完全隐藏。

将以上方案组合，我推荐的落地策略是：提示压缩与流式预填充为必选项，后端实施轻量文本压缩并开启增量预填充，前端配合分块传输，可将 10 秒延迟稳定压缩到 1.5 秒左右。
若对首字响应有极致要求，可进一步叠加推测式首 token。

最后补充多模态的特殊处理。多模态首 token 慢的根本原因同样在于 L 过大。应对所有图像与视频帧先进行视觉 token 压缩，压缩至数十 token/帧，再套用文本提示压缩；
对实时视频还可利用帧间时间冗余，仅计算增量 KV Cache。整体思路完全一致：缩小等效 L，重叠计算与等待时间。

# 模块二：Memory
## 2.你理解的 Agent memory 经典框架是什么？它的发展趋势是什么，最头部的玩家在怎么做？
### 回答
先看经典框架。目前业界公认的认知架构基础，来自CoALA，认知架构理论。这个理论把记忆分为两大类：短期工作记忆和长期记忆。长期记忆下面又细分为情景记忆、语义记忆和程序记忆。

基于这个理论，我认为所有经典的Agent Memory系统，本质上都遵循一个“写入—管理—读取”的循环。说得直白一点，就是智能体要先学会把经历存下来，然后自己管理这些记忆，最后在需要的时候准确读出来。

这里有三个核心设计:
第一，分层内存管理。这个思路借鉴了计算机操作系统的虚拟内存分页技术。智能体的上下文窗口是有限的，就像我们的内存条；而向量数据库这些外部存储，就像硬盘。
系统会自动把不常用的记忆从窗口里“交换”出去，存到外部，等需要时再调回来。这样一来，智能体就拥有了理论上无限长的记忆空间。
第二，自主管理与反思。这意味智能体不只是被动地存储，它自己能决定什么时候记、什么时候查、什么时候总结，甚至什么时候遗忘。更高级的是，它会定期进行“反思”——把零散的经历提炼成更高层次的洞察，存入长期记忆。
第三，记忆分层。不是所有记忆都同等重要。举个例子，MemGPT这个经典框架就把记忆分成了三层：核心记忆，比如当前的角色设定；回忆记忆，比如最近几轮对话；归档记忆，
比如长期积累的知识。不同层级使用不同的检索和管理策略。


那么，这套经典框架现在正在往哪些方向演进呢？我总结了五个明显的趋势。
第一个趋势，是从“存储”走向“认知”。 记忆系统不再是一个被动的数据库，而是主动的认知组件——它能压缩、能总结、能演化，甚至能主动遗忘无关信息。
第二个趋势，是“少即是多”正在成为共识。 以前大家觉得记忆越多越好，但现在发现，冗余的记忆反而会稀释有效信息。比如腾讯混元团队的研究表明，减少70%的记忆条目，信息密度反而能提升45%。所以，质量远比数量重要。
第三个趋势，是从通用走向专用。 代码编写、游戏NPC、个人助理……不同场景对记忆的要求完全不同，所以市场上出现了越来越多垂直定制的记忆方案。
第四个趋势，是工程化和标准化。 这个领域正在从学术研究走向工业落地，像BEAM、LoCoMo这样的基准测试已经出现，用来量化评估不同记忆系统的表现。
第五个趋势，是多智能体协同与隐私治理。 在多Agent系统中，记忆正在成为共享协作的基础；同时，数据隐私和所有权问题也被提到了前所未有的高度。

接下来，看看目前最头部的玩家分别是怎么做的。因为行业还没有统一标准，所以各家切入的角度不太一样，我挑六个有代表性的来说。
首先是 LangGraph，它来自LangChain生态。它的思路是把记忆当成工作流图里的“状态”来管理。短期记忆由线程内的状态负责，长期记忆则通过一个叫“Store”的模块实现。优势是和LangChain的深度绑定。
第二个是 MemGPT，现在也叫Letta。它把自己定位成一个操作系统级的记忆架构。智能体可以通过函数调用，自主管理四层记忆分区，强调的是“在Token空间里持续学习”的能力。
第三个是 Mem0，它走的是独立记忆层的路线，也就是Memory-as-a-Service。它使用向量加图结构的混合存储，在生产环境中实测能提升26%的准确率，同时降低91%的延迟，效果非常显著。
第四个是 腾讯混元的 Hy-Memory，它的核心理念就是刚才提到的“少即是多”。它设计了六层记忆框架，还引入了System1和System2的双系统分工，最大的创新是“supersedes演化链”——
新记忆不是覆盖旧记忆，而是通过版本链的方式进行演化。
第五个是 Zep 和 Graphiti，它们主打时间感知的知识图谱。它们能精确记录实体和事件之间的关系随时间的变化，特别适合处理动态信息。
最后一个叫 Second-Me，它的定位是个人AI分身，非常强调个性化。它采用层级记忆建模，而且数据默认存在本地，把隐私和控制权交给用户。

# 模块三：Task 
## 1.对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？
### 回答
首先，我们来审视问题的底层原理。为什么一个大模型在执行长时间任务时，会逐渐偏离甚至完全忘记最初的目标？原因可以从四个层面来理解。

第一，注意力衰减与“首尾”现象。Transformer的自注意力机制虽然具备全局视野，但事实上大量研究揭示，它对不同位置的关注度呈U形分布——序列开头和结尾的信息
往往获得较高关注，而中间部分则被严重忽略。如果目标指令不幸落在长上下文的中间位置，就极易被模型“遗忘”。
第二，位置编码的外推局限性。模型在训练时见过的序列长度是固定的。当推理时的上下文长度远超训练长度，位置嵌入会发生“失真”，导致模型难以准确建立远距离信息之间的关联。早期目标信号因此被逐步稀释。
第三，自回归机制缺乏持久记忆单元。标准Transformer没有显式的记忆结构，所有历史信息只能通过不断传递的KV缓存来保留。在逐层向上传递的过程中，旧token的表示
会被后续token不断冲击和覆盖，就像一场漫长的传话游戏，传得越远，原始目标的信息损耗就越严重。
第四，训练数据的偏差。预训练语料中，要求模型强依赖数百步之前指令的任务数据不够丰富，模型没有获得充足的训练来学会“时刻牢记初始目标”。

基于这些深层原因，业界主要发展出四类解决方案，我将逐一说明其原理与优劣。

第一类，位置编码与注意力机制的改造。
代表性方法包括RoPE的NTK感知缩放、ALiBi、xPos，以及Longformer、BigBird等稀疏注意力机制。
其原理是通过对位置编码进行插值或调整衰减因子，使模型在超出训练长度的序列上，仍能保持对远距离token的合理关注度。稀疏注意力则通过滑动窗口加全局节点的方式，
强制保留包含目标指令的全局token，避免其被淹没在信息流中。这类方案的优点在于相对轻量，无需改动模型主体结构。例如，基于LLaMA系列的模型，通过NTK-aware缩放
就能将上下文窗口外推至八千甚至更长。但其缺点也很明显：这只能缓解外推问题，无法根除“迷失在中间”现象；稀疏注意力可能丢失关键细节；且随着长度增加，计算开销依然巨大，长程依赖的衰减风险始终存在。

第二类，记忆增强的Transformer架构。
代表性的工作有Transformer-XL、Memorizing Transformers、MemTRM和RetNet等。
它们的基本思路是在模型内部引入显式的记忆机制。例如，Transformer-XL将前一片段的隐藏状态作为固定记忆传入当前片段，构建跨越片段的循环连接；
Memorizing Transformers则通过最近邻检索过去的键值对，将目标相关信息持续注入注意力计算过程。线性注意力结构如RetNet则简化了计算，使得维持长期记忆的成本不会随序列长度爆炸式增长。
这类架构的优点是，能够构建起跨越超长距离的显式信息流，理论上记忆不会因距离增加而自动消失。但缺点在于，它们引入了额外的训练成本和推理复杂度，比如最近邻检索会显著拖慢速度；
记忆容量始终有限，旧信息仍可能被挤出；而且需要专门设计的训练任务，否则模型未必善于利用这些记忆结构。

第三类，提示工程与智能体流程策略。
这是当前最实用的一类方法，包括目标定期重复、结构化阶段性总结、任务分解与ReAct、AutoGPT等智能体框架，以及MemGPT这样的虚拟记忆管理系统。
背后的原理很朴素：既然模型的注意力对末尾信息最为敏感，我们就每隔一定步数，把核心目标重新插入到最前方或最新位置；或者要求模型定期自我总结“当前正在执行的任务”，
并将总结与目标一并作为后续上下文的开头。更进阶的做法如MemGPT，它将上下文管理虚拟化为类似操作系统的“主存—工作记忆—存档”层次，由模型自主决策何时将目标提取为常驻信息，从而避免被遗忘。
这一类方案最大的优点是无需修改模型本身，效果立竿见影，且能与任何大模型配合。MemGPT等甚至可以突破固定上下文窗口的限制，实现超长序列的任务处理。缺点则在于，
频繁重复目标会大量消耗宝贵的上下文token；整个机制高度依赖模型自身的管理能力，可能出现决策失误；多步工具调用也会引入延迟与误差累积。

第四类，外部记忆与检索增强生成（RAG）。
该方法借助向量数据库存储历史信息，并按需检索目标任务与中间成果。
原理是，不再将全部信息保存在瞬时上下文中，而是将任务目标、关键里程碑等存入外部向量存储。一旦模型表现出遗忘倾向，检索模块就取回相关记忆，注入当前上下文，相当于为模型提供了一个外部记事本。
其优点是，上下文得以保持干净且长度可控，检索出的记忆可以非常精准，并可与长期任务管理紧密结合。缺点则是，检索的时机和内容选择高度影响最终效果，容易引入无关信息；系统延迟与复杂度显著增加；
而且它无法解决模型在瞬时上下文内整合新旧信息的根本能力瓶颈。

# 模块四：Tool / Session Runtime
## 1.Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？
### 回答
首先，理解问题的根源。智能体所调用的工具有同步和异步两类。同步工具会阻塞当前执行流，直到结果返回。但异步工具往往涉及耗时操作，比如生成一份复杂报告，
或者调用外部长时间运行的接口。我们不能让用户一直等待，但异步工具产生的结果同样至关重要，必须可靠地送达智能体并最终呈现给用户。这就形成一个核心矛盾：
输入输出的完成时间不可控，而调用方持有的线程或用户的注意力，都不能被无限期地阻塞。

要解决这个矛盾，就必须深入到操作系统和编程模型的底层去。

在操作系统层面，同步调用意味着线程主动陷入内核等待，比如执行read操作，直到数据就绪，这期间CPU可能空转或被让出，但调用链是线性的。而异步调用本质上是“提
交任务”与“获取结果”这两步操作的解耦。提交之后，调用方会立即拿回一个句柄，随后底层通过三种机制之一来发出完成通知。第一种是硬件中断，数据就绪时触发中断，由
CPU回调处理程序；第二种是事件循环配合回调，比如Linux的epoll或Windows的IOCP，监控文件描述符的状态，就绪后调用我们注册的函数；第三种是编程语言层面提供的
Future或Promise，将回调封装成可组合的状态对象，允许协程在await点上挂起，待结果就绪后再被恢复。

了解了这些原理，我们就清楚地知道，设计异步工具的通知机制，实质上就是在构建一个可靠、低延迟、可伸缩的状态同步系统。通知的本质只有两条路：要么拉，也就是调用方轮
询句柄的状态；要么推，也就是执行方在完成时主动触发一个事先约定好的操作，无论是回调、消息还是中断。到底选择哪一种，取决于通信成本、实时性要求，以及系统边界是处于同一进程、跨进程还是跨网络。

基于以上原理，就可以构建一套完整的异步执行架构。我将这套架构抽象为三层：智能体运行时层、工具执行层，以及位于二者之间的通知中间件。

在工具定义阶段，每一个工具需要显式声明自己的异步语义。当智能体决定调用一个异步工具时，框架会按照以下步骤展开工作。
第一步，提交任务并获取句柄。工具调用不会阻塞事件循环，而是立即返回一个包含任务标识的句柄对象。同时，真正的任务体被序列化后发送到任务队列，交由后台的工作节点执行。
第二步，生成中间响应并释放连接。框架将当前状态打包为一个“等待中”的状态。它会保存对话上下文和推理步骤的检查点，将状态持久化，同时向前端推送一条“正在处理”的通知。
关键之处在于，智能体的协程此时会主动挂起，对应的HTTP长连接也可以从容地降级为短连接。我们不再强制维持连接，而是先返回一个中间状态，后续通过推送或通知来重新唤醒流程。
第三步，注册通知监听。框架以任务标识为键，在通知中间件中注册一个待处理的回调。
第四步，结果到达后恢复执行。一旦收到任务完成的通知，框架就会加载之前保存的检查点，将结果精确注入到工具调用的返回位置，让智能体从挂起点继续推理。最终生成的回复，再推送给用户。

在这样一个流程中，最核心的设计要点，就在于“完成通知”的具体实现。
我想的是可以在进程内进行异步，直接利用编程语言协程的挂起与恢复能力。如果工具的实现就在同一个进程内，比如发起一个异步HTTP请求，我们可以将协程包装成一个Future对
象并立即返回。智能体在await这个Future时挂起，事件循环则继续处理其他请求。当底层I/O完成，事件循环会把结果送入协程，智能体得以无缝继续执行。但是这种方式的局限在于，
当系统走向分布式，跨进程甚至跨网络时，我们就必须采用推送模式，也就是推模式。这背后需要解决一个核心问题：如何让执行方能够寻址到调用方，并触发其逻辑。

比较常见的做法是 ：消息队列的发布订阅机制，这也是生产环境中最推荐的做法。智能体提交任务后，订阅一个特定的完成频道，比如使用Redis的频道。任务完成后，工作节
点将结果消息发布到该频道。智能体侧的订阅者收到消息，将结果写回会话存储，并通过内部事件循环唤醒挂起的推理流程。这种模式需要我们关注几个关键点：基于任务标识的幂等
消费，防止重复注入结果；使用持久化队列确保消息不丢失；以及设定任务超时时间，超时后自动发送失败通知，避免智能体永久挂起。

最后，异步工具的结果必须完整地呈现给用户，这需要一个可靠的会话层推送机制。如果用户一直保持连接，比如通过WebSocket或SSE，智能体在生成最终回复后，可以直接通过该连接
推送完整消息。对用户而言，体验是“先收到一条处理中的提示，紧接着获得完整答案”。如果用户已经断开连接，那么结果和智能体的回复将被持久化到会话历史中。用户下次重新连接
或主动拉取时，可以立即获取。这就是“异步完成”与“拉取式通知”的有机结合。

总的来说，设计异步工具的执行与通知，需要遵循四条原则。第一，永远分离提交与结果，让调用方立即获得句柄，绝不阻塞当前线程或协程。第二，通知方式按系统边界进行选
取：进程内用await，分布式用消息队列的发布订阅，并始终附加超时、重试和去重机制。第三，让智能体状态可恢复，异步调用时主动保存检查点，收到通知后再恢复运行，
绝不依赖易失的内存。第四，面向用户暴露进度而非空白等待，先告知“正在处理”，随后主动推送最终结果，允许用户在离线后拉取。异步的结果始终重要，而好的设计正是用状态机和推送通道，把“等待”从用户的感官中彻底剥离。

# 模块五：Agent Runtime 架构对比
## 1.Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？
### 回答
首先这两种设计，从消息结构上看就完全不一样。
在 Claude 原生的工具使用协议中，工具调用被定义为消息内容块的一部分。在一条 assistant 消息内，模型的输出可以同时包含文本块和工具调用块。也就是说，
模型能够一边对用户说话，一边发起工具请求。比如，模型可以说“我先帮您查一下天气”，并在同一条回复中发出查询指令。而当工具执行完毕后，执行结果并不作为一个独
立的工具角色返回，而是被封装在紧随其后的一条 user 消息中，以 tool_result 内容块的形式出现，并明确引用之前的调用编号。这强制形成了严格的 user 与 assistant 交替。
值得注意的是，工具返回的结果不仅是字符串，还可以是一个内容块数组，这允许直接返回图像、截图等多模态数据，这也正是 Claude 计算机操作与代码助手能够进行视觉反馈的重要基础。

而国内模型普遍兼容的 OpenAI function calling 协议，设计思路完全不同。在 assistant 消息中，工具调用并不存在于正文内容里，而是放在一个单独的顶层字段 tool_calls 中。
此时，正文内容通常为空。模型在调用工具时往往是沉默的。而工具的执行结果，则通过一个独立的 role: tool 的消息返回，拥有自己的 tool_call_id。
这样一来，对话序列中会打破 user 与 assistant 的严格交替，出现 user、assistant带工具调用、多条tool消息、再回到assistant的局面。
此外，工具消息的内容只能是纯文本，无法直接返回图片等多媒体信息。

同时，这两种设计各有优缺点。
Claude 设计的优点非常鲜明。
第一，它能实现“说”与“做”的自然融合，模型在调用工具时可以同步向用户解释意图，交互体验更为流畅，避免了机器人突然静默操作的割裂感。
第二，它提供了严格的安全边界。所有工具返回的外部信息，都被当作“用户提供的内容”注入对话，这在架构层面降低了工具输出被模型误认为系统指令的风险，对提示注入攻击有很强的结构免疫力。
第三，原生支持多模态的工具反馈，工具可以带回图像、图表，这在需要视觉感知的场景中具有不可替代的价值。第四，所有信息都被统一为内容块，为未来的交互形态提供了高度可扩展的统一抽象。
Claude 设计的缺点则主要体现在生态层面。
第一，生态兼容性较弱。大多数语言模型框架和工具链最初都围绕 OpenAI 格式构建，要接入 Claude 原生协议需要专门适配，增加了集成成本。
第二，对开发者而言，处理混合内容数组比简单判断 tool_calls 是否存在要复杂，构造带有工具结果的用户消息也更容易出错，心智负担稍高。
第三，严格的用户与助手交替在某些极端情况下不够灵活，虽然可以将多个工具结果打包在同一条用户消息里，但仍需要开发者在工程上适应。

再来看 OpenAI 兼容协议的优缺点。
它最大的优点是已经成为了事实标准，生态无比强大。主流的 Agent 框架、中间件和网关都原生支持这种格式，国内模型只需兼容此协议，就能零成本融入现有工具链，极大地降低了开发者的迁移门槛。
其次，结构简单清晰，工具调用独立存放，工具结果以专用角色返回，使对话历史的角色划分直观明了，便于调试和状态管理。
另外，在多轮顺序调用中，连续发送多条 tool 消息再让模型总结，也给开发者带来了更高的工程控制自由度。

但它的缺点同样不可忽视。
首先，“说”与“做”是分离的，调用工具时模型通常只能保持沉默，无法提前向用户沟通正在进行的操作，交互显得生硬。虽然规范上允许非空的正文内容，但实际模型很少同时输出文本和调用，错失了混合输出的机会。
其次，缺乏原生的多模态工具反馈，工具不能直接返回图像，如果想让模型看图，必须额外用一条用户消息手动注入图片，增加了复杂度和延迟。
最后，独立的 tool 角色在安全边界上存在潜在脆弱性，它既非用户也非助手，容易成为提示注入的薄弱环节。虽然可以通过训练和护栏机制来缓解，但结构上不如 Claude 将所有外部输入都视为用户内容那样强健。

综合来看，Claude 的设计更像一个以内容块为基元的统一对话模型，追求更自然的人机交互、更稳固的安全结构和多模态的扩展性，代价是生态适配的成本和一定的实现复杂度。
而 OpenAI 兼容的 function calling，则是一种极简化、工程化的调度模型，以角色为核心，凭借生态的力量迅速成为通用语言。
国内模型选择这条路径，本质上是用最小的摩擦换取最大的可用生态，尽管在交互丰富性和安全性结构上做出了妥协。
